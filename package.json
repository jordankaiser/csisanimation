{
  "name": "promise",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build",
    "rebuild": "rm -rf .parcel-cache && npm run build && npm run start"
  },
  "devDependencies": {
    "buffer": "^6.0.3",
    "parcel": "^2.9.3"
  },
  "dependencies": {
    "gsap": "^3.12.5",
    "video.js": "^8.17.3"
  }
}
