{
  "name": "promise",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build",
    "rebuild": "rm -rf .parcel-cache && npm run build && npm run start"
  },
  "devDependencies": {
    "parcel": "^2.9.3"
  },
  "dependencies": {
    "gsap": "^3.12.5"
  }
}
